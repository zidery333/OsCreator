---
name: {{SLUG}}
description: {{TITLE}}. Use when <the exact words a person would actually say>.
---

# {{TITLE}}

## Do this
1. 
2. 
3. 

## Rules
- 

## Done when
- 

<!--
  The `description:` line above is the important one — it decides whether this
  ever runs. Lead with what it does, then the words someone would say to ask for it.

  Other settings you can add between the --- lines:
    argument-hint: [thing]     shown while someone is typing /{{SLUG}}
    allowed-tools: Bash(git *) tools this skill may use without asking
  Leave the model unnamed unless this job truly needs a particular one — then it
  runs on whatever the conversation is already using, and gets better over time.

  Keep this under about 120 lines. Anything longer belongs in a second file in
  this folder, mentioned here so it only gets read when it's needed.
-->
